use compiler::helpers::add_instruction;

pub fn compile_if0(branch1: String, branch2: String) -> String {
    let mut compiled: String = "".to_string();

    compiled
}

pub fn compile_call(func: String) -> String {
    let mut compiled: String = "".to_string();

    compiled
}

pub fn compile_lam(lam: String) -> String {
    let mut compiled: String = "".to_string();

    compiled
}
