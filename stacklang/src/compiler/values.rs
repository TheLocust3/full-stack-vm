use compiler::helpers::add_instruction;

pub fn compile_variable(var: String) -> String {
    let mut compiled: String = "".to_string();

    compiled
}

pub fn compile_number(num: String) -> String {
    num
}

pub fn compile_thunk(compiled_thunk: String) -> String {
    let mut compiled: String = "".to_string();

    // todo: add areas in memory that contain thunk code, push address to stack

    compiled
}
